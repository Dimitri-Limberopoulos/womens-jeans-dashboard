# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Excel lock files (created when a workbook is open)
~$*.xlsx
~$*.xls

# Local dashboard backups (timestamped)
index.html.bak_*
cross_retailer_dashboard*.bak_*

# Browser profile (used by levis_scraper.py for sticky session)
.levis_browser_profile/

# Intermediate scrape state — keep, but allow override
# (commented out so progress files stay versioned for resumability)
# *_pdp_progress.json
